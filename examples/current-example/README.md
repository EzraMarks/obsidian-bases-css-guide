# Current Example

## Setup Instructions

1. Add both CSS files to your Obsidian CSS snippets (Settings → Appearance → CSS snippets):
   - `bases-core-styles.css` (required framework styles)
   - `bases-music-styles.css` (music-specific styling)
2. Place the `Music Library` folder anywhere in your Obsidian vault
3. Open the contained Base and explore!